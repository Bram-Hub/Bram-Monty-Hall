<?php

use App\Models\Monty;
use App\Models\Game;

/*
$monty = new Monty();
$monty->type = "normal";
$monty->total_wins = 0;
$monty->total_losses = 0;

$monty->save();
*/

// $monty = Monty::findOrFail(1);
// echo($monty->type);

// $monty->games()->create(['cookie_id' => 111, 'outcome' => true]);

//$monty = new Monty();
//$x = $monty -> addMonty(array('type' => 'xx', 'total_wins' => 0, 'total_losses' => 0, 'switched_times' => 0, 'game_nums' => 0));
//$y = $monty -> delMonty(array('type' => 'xx'));
//$monty -> updMonty('id', 1, array('type' => 'yy'));
// echo($monty -> oneMonty('id', 1));
